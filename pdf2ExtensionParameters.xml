<dummy>    
	<param 
        name="pdf2.ug.logo" 
        expression="${pdf2.ug.logo}" 
        if="pdf2.ug.logo"/>

    <param name="pdf2.ug.chapter.header" 
        expression="${pdf2.ug.chapter.header}" 
        if="pdf2.ug.chapter.header">
    </param>
    
    <param name="pdf2.ug.chapter.minitoc.layout" 
        expression="${pdf2.ug.chapter.minitoc.layout}" 
        if="pdf2.ug.chapter.minitoc.layout"/>
</dummy>